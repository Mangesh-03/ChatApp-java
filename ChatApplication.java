import utilx.ClientGUI.ChatClient;
import utilx.ServerGUI.ChatServer;

class ChatApplication
{
    public static void main(String a[])
    {
        ChatServer sr = new ChatServer();
        ChatClient obj = new ChatClient();
    }
}